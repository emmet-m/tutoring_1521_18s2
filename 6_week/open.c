#include <stdio.h>
#include <stdlib.h>
#include <fcntl.h>

/*
 * List of main open codes:
 *  
 * O_RDONLY
 * O_WRONLY
 * O_CREAT
 * O_APPEND
 * O_TRUNC
 * O_RDWR
 */

int main (int argc, char * argv, char * envp) {

    
    // fopen(FilePath, "r");

    // fopen(FilePath, "a");

    // fopen(FilePath, "w");

    // fopen(FilePath, "r+");

    // fopen(FilePath, "w+");

    return 0;
}
